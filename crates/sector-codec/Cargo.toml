[package]
name = "sector-codec"
version.workspace = true
edition.workspace = true
description = "Detection and repair primitives for SECTOR: block CRC32, replication, and RS(n,k) erasure coding over GF(2^8)"
license.workspace = true
repository.workspace = true
homepage.workspace = true
keywords = ["crc", "reed-solomon", "erasure-coding", "no-std", "gf256"]
categories = ["encoding", "algorithms", "no-std::no-alloc", "embedded"]

[dependencies]

